---
host_configuration:
  vcenter:
    ip_address: "172.16.20.106"    
    cluster_name: "SIL_Dev-Cluster"    
    datacenter: "SIL_Datacenter"
  storage:    
    datastore: "NVMe Storage"
    folder: "Testing"
  iso_folder_path: "/root/"

kit:  
  kickstart_configuration:
      netmask: "255.255.255.0"

      # Gateway will probably be different on a per kit basis (depends on the vlan)
      gateway: "172.16.82.1"
      # Note root_password is specified under VM_settings.

  kit_configuration:
      # The branch we want to pull from.
      branch_name: devel

      # these branches are name of the branches on your fork you want to use
      tfplenum_branch_name: devel

      source_repo: labrepo

      use_ceph_for_pcap: false

      # Only fill this in if use_ceph_for_pcap is true
      moloch_pcap_storage_percentage:
      elasticsearch_cpu_percentage:
      elasticsearch_ram_percentage:
      logstash_server_cpu_percentage:
      logstash_replicas:
      es_storage_space_percentage:

      # Kubernetes cidr will probably be different on a per kit basis
      kubernetes_cidr: "172.16.82.176"

      home_nets:
          - "172.16.82.0/24"

      # This is optional - you can fill it in if you need to
      external_nets:

      kafka_cpu_percentage:
      moloch_cpu_percentage:
      bro_cpu_percentage:
      suricata_cpu_percentage:
      zookeeper_cpu_percentage:

      ideal_es_cpus_per_instance:
      es_cpu_to_memory_ratio_default:

  VMs:
    # Note: You should not have more than one of these
    test4-controller.lan:
      type: "controller" # Can be controller, sensor, or server

      # GestOS should be one of the enumeration values in com.vmware.vcenter.vm.GuestOS
      # https://vdc-repo.vmware.com/vmwb-repository/dcr-public/89baa2e8-e8f1-4d6a-87c5-baa3116e422a/42afa517-2614-43fb-a352-c3ed88ae95fc/doc/com/vmware/vcenter/vm/class-use/GuestOS.html
      # For us that will likely be: RHEL_7_64, CENTOS_7_64, VMWARE_PHOTON_64, or UBUNTU_64
      # Sorry Holden, you're on your own for arch. They didn't have it in the guest list
      vm_guestos: CENTOS_64
      vm_to_clone: "CentOS Template"
      domain: 'lan'
      gateway: "172.16.82.1"
      dns:
        - "172.16.82.1"
      networking:
        std_portgroup_name: "VM Network"
        nics:
          ens192:
            type: "manual" # This can be auto, manual, or link-local
            ip_address: "172.16.82.160"
            subnet_mask: "255.255.255.0"
            start_connected: True
            management_interface: True
            monitoring_interface: False
            mac_auto_generated: True
            mac_address: "" # This is ignored if mac_auto_generated is true
            dv_portgroup_name: "82 Portgroup"
            std_portgroup_name:
          ens224:
            type: "link-local" # This can be auto, manual, or link-local
            ip_address: "169.254.251.118"
            subnet_mask: "255.255.0.0"
            start_connected: True
            management_interface: False
            monitoring_interface: True
            mac_auto_generated: True
            mac_address: "" # This is ignored if mac_auto_generated is true
            dv_portgroup_name: "Monitoring Portgroup"
            std_portgroup_name:
      cpu_spec:
        sockets: 4
        cores_per_socket: 1
        hot_add_enabled: False
        hot_remove_enabled: False
      memory_spec:
        size: 8 # This value is in GiB
        hot_add_enabled: False
      disks:
        boot: 200 # Name of disk followed by capacity in GiBs
      iso_file: "rhel-server-7.4-x86_64-dvd.iso"
      boot_order: # Valid options are CDROM, DISK, and ETHERNET
        - "CDROM" # CDROM will be ignored if iso_file is None
        - "DISK"
        - "ETHERNET"
    test4-server1.lan:
      type: "master-server" # Can be controller, sensor, or server
      vm_guestos: CENTOS_64
      networking:
        nics:
          ens192:
            type: "manual"
            ip_address: "172.16.82.161"
            start_connected: True
            management_interface: True
            monitoring_interface: False
            mac_auto_generated: True
            mac_address: ""
            dv_portgroup_name: "82 Portgroup"
            std_portgroup_name:
      cpu_spec:
        sockets: 4
        cores_per_socket: 1
        hot_add_enabled: False
        hot_remove_enabled: False
      memory_spec:
        size: 8
        hot_add_enabled: False
      boot_drive_name: "sda"
      # These are the drives selenium will select.
      ceph_drives:
        - sdb
        - sdc
      disks:
        boot: 20
        ceph: 20
        ceph2: 20
      iso_file:
      boot_order:
        - "CDROM"
        - "DISK"
        - "ETHERNET"
    test4-sensor1.lan:
      type: "sensor" # Can be controller, sensor, or server
      vm_guestos: CENTOS_64
      networking:
        nics:
          ens192:
            type: "manual"
            ip_address: "172.16.82.162"
            start_connected: True
            management_interface: True
            monitoring_interface: False
            mac_auto_generated: True
            mac_address: ""
            dv_portgroup_name: "82 Portgroup"
            std_portgroup_name:
          ens224:
            type: "manual"
            ip_address:
            start_connected: True
            management_interface: False
            monitoring_interface: True
            mac_auto_generated: True
            mac_address: ""
            dv_portgroup_name: "Monitoring Portgroup"
            std_portgroup_name:
      cpu_spec:
        sockets: 4
        cores_per_socket: 1
        hot_add_enabled: False
        hot_remove_enabled: False
      memory_spec:
        size: 8
        hot_add_enabled: False
      boot_drive_name: "sda"
      # Selects the ceph or pcap drives we will use during kit configuration
      # A sensor can have ceph_drives if the use_ceph_for_pcap is set to true.
      # otherwise you should use pcap_drives instead.
#            ceph_drives:
#              - sdb
      pcap_drives:
        - sdb
      disks:
        boot: 20
        ceph: 20
      iso_file:
      boot_order:
        - "CDROM"
        - "DISK"
        - "ETHERNET"
