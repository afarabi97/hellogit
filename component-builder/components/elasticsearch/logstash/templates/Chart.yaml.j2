apiVersion: v1
name: logstash
home: https://bitbucket.di2e.net/projects/THISISCVAH/repos/tfplenum/browse/charts/logstash
version: {{ component["helm_version"] }}
appVersion: {{ component["app_version"] }}
description: Logstash is an open source, server-side data processing pipeline that ingests data from a multitude of sources simultaneously, transforms it, and then sends it to your favorite stash.
annotations:
  pmoSupported: true
  isSensorApp: false
