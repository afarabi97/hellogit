---
- name: hostvar fqdn
  debug: var=hostvars[inventory_hostname].ansible_fqdn

- name: create list
  set_fact:
    host_list: "{{ hostvars[inventory_hostname].groups['mips'] }}"

- name: Copy over hosts template
  template:
    src: templates/hosts.j2
    dest: "/etc/hosts"
...
