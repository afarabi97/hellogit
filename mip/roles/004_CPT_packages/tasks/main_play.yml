---
- name: Install packages only required for CPT core
  dnf:
    name: "{{ item }}"
    state: present
  loop: "{{ cpt_packages }}"

- name: Create hidden file for validation if playbooks crash
  file:
    path: /operator/.DO_NOT_DELETE/.004
    state: touch
...
