apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}
  labels:
    component: mattermost
spec:
  replicas: 1
  selector:
    matchLabels:
      component: mattermost
  template:
    metadata:
      labels:
        component: mattermost
    spec:
      initContainers:
      - name: mattermost-init
        image: "busybox:1.31.1"
        securityContext:
          runAsUser: 0
        command:
        - "/bin/sh"
        - "-c"
        - "chown -R 2000:2000 /mattermost-data"
        volumeMounts:
        - name: data
          mountPath: /mattermost-data
          subPath: app
      containers:
      - name: mariadb
        image: {{ .Values.mariadbImage }}
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 3306
        env:
        - name: MYSQL_ROOT_PASSWORD
          value: {{ .Values.sqlRootPassword }}
        - name: MYSQL_DATABASE
          value: {{ .Values.sqlDatabase }}
        - name: MYSQL_USER
          value: {{ .Values.sqlUsername }}
        - name: MYSQL_PASSWORD
          value: {{ .Values.sqlPassword }}
        volumeMounts:
        - name: data
          mountPath: /var/lib/mysql
          subPath: database
      - name: mattermost
        image: {{ .Values.mattermostImage }}
        imagePullPolicy: IfNotPresent
        command:
        - "/bin/sh"
        - "-c"
        - "/mattermost/config_file.sh; /mattermost/setup.sh & /entrypoint.sh mattermost"
        ports:
        - containerPort: 8065
        env:
        - name: DB_HOST
          value: localhost
        - name: DB_PORT_NUMBER
          value: "3306"
        - name: MM_DBNAME
          value: {{ .Values.sqlDatabase }}
        - name: MM_USERNAME
          value: {{ .Values.sqlUsername }}
        - name: MM_PASSWORD
          value: {{ .Values.sqlPassword }}
        - name: MM_SQLSETTINGS_DRIVERNAME
          value: "mysql"
        - name: MM_SQLSETTINGS_DATASOURCE
          value: {{ .Values.sqlUsername }}:{{ .Values.sqlPassword }}@tcp({{ .Values.sqlHost }}:3306)/{{ .Values.sqlDatabase }}?charset=utf8mb4,utf8@readTimeout=30s&writeTimeout=30s
        - name: ADMIN_USER
          valueFrom:
            secretKeyRef:
              name: {{ .Release.Name }}-secret
              key: admin_user
        - name: ADMIN_EMAIL
          valueFrom:
            secretKeyRef:
              name: {{ .Release.Name }}-secret
              key: admin_email
        - name: ADMIN_PASS
          valueFrom:
            secretKeyRef:
              name: {{ .Release.Name }}-secret
              key: admin_pass
        - name: TEAM_NAME
          valueFrom:
            secretKeyRef:
              name: {{ .Release.Name }}-secret
              key: team_name
        # - name: OPEN_ID_SECRET
        #   valueFrom:
        #     secretKeyRef:
        #       name: mattermost-openid
        #       key: openid-secret
        volumeMounts:
        - name: data
          mountPath: /mattermost/data
          subPath: app
        - name: setup
          mountPath: /mattermost/setup.sh
          subPath: setup.sh
        - name: setup
          mountPath: /mattermost/config_file.sh
          subPath: config_file.sh
        - name: webca
          mountPath: /etc/ssl/certs/container
          readOnly: true
      nodeSelector:
        role: "server"
      volumes:
      - name: setup
        configMap:
          name: {{ .Release.Name }}-setup
          defaultMode: 0777
      - name: webca
        secret:
          defaultMode: 420
          optional: false
          secretName: {{ .Release.Name }}-certificate
      - name: data
        persistentVolumeClaim:
          claimName: {{ .Release.Name }}-pvc
      # - name: mattermost-saml
      #   secret:
      #     secretName: mattermost-saml
      #     defaultMode: 0444
      # - name: sso-idp-metadata
      #   secret:
      #     secretName: sso-idp-metadata
      #     defaultMode: 0444
