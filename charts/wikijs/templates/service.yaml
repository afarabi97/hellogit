apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  labels:
    component: {{ .Release.Name }}
spec:
  selector:
    component: {{ .Release.Name }}
  ports:
  - name: {{ .Release.Name }}-https
    port: 443
    targetPort: 3443
    protocol: TCP
  type: LoadBalancer
