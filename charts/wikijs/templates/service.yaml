apiVersion: v1
kind: Service
metadata:
  name: wikijs
  labels:
    component: wikijs
spec:
  selector:
    component: {{ .Release.Name }}
  ports:
  - name: wikijs-http
    port: 80
    targetPort: 3000
    protocol: TCP
  type: LoadBalancer
