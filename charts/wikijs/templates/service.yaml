apiVersion: v1
kind: Service
metadata:
  name: wikijs
  labels:
    component: wikijs
spec:
  selector:
    component: {{ .Release.Name }}
  ports:
  - name: wikijs-https
    port: 443
    targetPort: 3443
    protocol: TCP
  type: LoadBalancer
