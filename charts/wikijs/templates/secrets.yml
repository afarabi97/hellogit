---

apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-secret
data:
  adminEmail: {{ .Values.adminEmail | b64enc | quote }}
  adminPassword: {{ .Values.adminPassword | b64enc | quote }}
