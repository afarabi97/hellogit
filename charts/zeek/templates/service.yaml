---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  labels:
    component: zeek
spec:
  ports:
  - port: 9092
    name: server
  selector:
    component: zeek
    deployment: {{ .Release.Name }}
