apiVersion: cert-manager.io/v1alpha3
kind: Certificate
metadata:
  name: {{ .Release.Name }}-metricbeat
  annotations:
    "helm.sh/hook": pre-install
    "helm.sh/hook-weight": "-10"
    "helm.sh/hook-delete-policy": "before-hook-creation"
spec:
  # Secret names are always required.
  secretName: {{ .Release.Name }}-metricbeat-certificate
  duration: 8760h # 365d
  renewBefore: 360h # 15d
  commonName: {{ .Release.Name }}-metricbeat
  subject:
    organizations:
    - "CVA/H"
    countries:
    - "US"
  isCA: false
  keySize: 2048
  keyAlgorithm: rsa
  keyEncoding: pkcs1
  usages:
    - client auth
  issuerRef:
    name: webca-issuer
    kind: Issuer
    group: cert-manager.io
