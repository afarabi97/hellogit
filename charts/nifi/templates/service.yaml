apiVersion: v1
kind: Service
metadata:
  labels:
    component: nifi
  name: nifi
spec:
  ports:
  - name: https
    port: 443
    targetPort: 443
  selector:
    component: {{ .Release.Name }}
  type: LoadBalancer
