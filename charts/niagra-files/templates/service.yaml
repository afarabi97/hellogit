apiVersion: v1
kind: Service
metadata:
  labels:
    component: {{ .Release.Name }}
  name: {{ .Release.Name }}
spec:
  ports:
  - name: https
    port: 443
    targetPort: 443
  selector:
    component: {{ .Release.Name }}
  type: LoadBalancer
