apiVersion: v1
kind: Service
metadata:
  labels:
    component: {{ .Release.Name }}
  name: {{ .Release.Name }}
spec:
  ports:
  - name: "webgui"
    port: 80
    targetPort: {{ .Values.nifi_port }}
  selector:
    component: {{ .Release.Name }}
  type: LoadBalancer
