---
apiVersion: v1
kind: Service
metadata:
  name: rocketchat
  labels:
    component: rocketchat
spec:
  selector:
    component: {{ .Release.Name }}
  ports:
  - name: https
    port: 443
    protocol: TCP
    targetPort: https
  type: LoadBalancer

