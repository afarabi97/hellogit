apiVersion: v1
kind: Service
metadata:
  labels:
    component: jcat-nifi
  name: jcat-nifi
spec:
  ports:
  - name: "webgui"
    port: 80
    targetPort: {{ .Values.jcat_port }}
  selector:
    app: jcat-nifi
  type: LoadBalancer
