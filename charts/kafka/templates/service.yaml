---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  labels:
    component: kafka
spec:
  ports:
  - port: 9092
    name: server
  selector:
    component: kafka
    deployment: {{ .Release.Name }}
