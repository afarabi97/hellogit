---
apiVersion: v1
kind: Service
metadata:
  labels:
    component: moloch
  name: moloch
spec:
  ports:
  - name: "webgui"
    port: 443
    targetPort: https
  selector:
    app: moloch-viewer
  type: LoadBalancer
