# {{ ansible_managed }}
{{ server_ip }} {{ ansible_controller_hostname }}

# Network nodes as generated through Ansible.
{% for host in groups['servers'] %}
{% if 'ip' in hostvars[host] %}
{{ hostvars[host]['ip'] }}  {{ host }} {{ hostvars[host]['inventory_hostname_short'] }}
{% endif %}
{% endfor %}
