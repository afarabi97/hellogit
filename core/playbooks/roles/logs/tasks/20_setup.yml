---

- name: Deploy
  include_role:
    name: kubernetes/common
    tasks_from: kube_create
  vars:
    name: "filebeat"
    file_name: "{{ logs_dir }}/ds-bro.yml"
  when: groups['bro'] | length > 0

...
