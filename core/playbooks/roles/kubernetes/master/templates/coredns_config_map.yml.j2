.:53 {
    log
    errors
    health
    kubernetes cluster.local
    forward . {{ hostvars[groups['master_server'][0]].management_ipv4 }}
    cache 30
    loop
    reload
    loadbalance
    ready :8181
}
