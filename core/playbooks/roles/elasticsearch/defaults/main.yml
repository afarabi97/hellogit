---
#### Main Role Configuration ####

es_dir: "{{ install_dir  }}/elastic"
es_upgrade_ver: "{{ elastic_7_version }}"
elastic_dir: "{{ install_dir  }}/elasticsearch"
elastic_user: "1000"
elastic_group: "1000"

#### install path
es_data_path: "{{ data_path }}/elastic"
logstash_user: "logstash_internal"
default_elastic_user: elastic
default_data_dir_per_server: 3

filebeat_modules:
  - apache
  - auditd
  - haproxy
  - icinga
  - iis
  - kafka
  - mongodb
  - mysql
  - nats
  - nginx
  - osquery
  - postgresql
  - redis
  - santa
  - system
  - traefik
  - activemq
  - cef
  - cisco
  - coredns
  - envoyproxy
  - ibmmq
  - iptables
  - misp
  - mssql
  - panw
  - rabbitmq
  #- zeek
  #- suricata
  #- netflow
