{% set index_rollover_size = 90 %}
{% if kit_size > 1 %}
{% set index_rollover_size = (groups['servers']|length * 3) * 50 %}
{% endif %}
{
  "policy" : {
    "phases" : {
      "hot" : {
        "min_age" : "0ms",
        "actions" : {
          "rollover" : {
            "max_size" : "{{ index_rollover_size }}gb",
            "max_age" : "365d"
          }
        }
      }
    }
  }
}
