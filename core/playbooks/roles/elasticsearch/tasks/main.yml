######################################################
################# Setup Elasticsearch ################
######################################################
---

- import_tasks: 00_preflight.yml
  when: inventory_hostname in groups['master_server']

- import_tasks: 10_storage.yml

- import_tasks: 15_master.yml
  when: separate_master and inventory_hostname in groups['master_server']

- import_tasks: 20_data.yml
  when: inventory_hostname in groups['master_server']

# - import_tasks: 25_ingest.yml
#   when: separate_ingest and inventory_hostname in groups['master_server']

# - import_tasks: 30_search.yml
#   when: separate_search and inventory_hostname in groups['master_server']

#- import_tasks: 50_license.yml
#  when: install_license and inventory_hostname in groups['master_server']
