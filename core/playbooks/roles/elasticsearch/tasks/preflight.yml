---

- set_fact:
    es_installed: false

- name: Checking if Elastic is already deployed
  shell: helm list --all | grep -c elastic
  register: es_results
  failed_when: es_results.rc >= 2

- set_fact:
    es_installed: true
  when: es_results is defined and es_results.stdout|int > 0

