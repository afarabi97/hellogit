---

- include_tasks: 01_prepare.yml
  when: inventory_hostname in groups['master_server']

- block:
  - include_tasks: 05_files.yml
    when: inventory_hostname in groups['master_server']

  - include_tasks: 06_plugins.yml
    when: inventory_hostname in groups['logstash']

  - include_tasks: 20_setup.yml
    when: inventory_hostname in groups['master_server']
  when: groups['logstash'] | length > 0

...
