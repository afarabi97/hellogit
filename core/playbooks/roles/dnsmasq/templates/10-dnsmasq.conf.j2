bind-interfaces
expand-hosts
local=/{{ kit_domain }}/
domain={{ kit_domain }}
interface={{ management_interface }}
hostsdir={{ host_dir }}
listen-address={{ hostvars[groups['master_server'][0]].management_ipv4 }},127.0.0.1
