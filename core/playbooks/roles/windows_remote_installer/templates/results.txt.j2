{#  This template describes a file that will contain the output of a 
    docker_container task that registers the results in the variable 
    install_results. #}
{{ install_results.ansible_facts.docker_container.Output }}