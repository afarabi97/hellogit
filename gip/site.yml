---
- name: A playbook to setup samba, tftp, chrony.
  hosts: gipsvc
  become_user: root
  roles:
    - { role: "clean-yum", tags: ["clean-yum"]}
    - { role: "samba", tags: ["samba"] }
    - { role: "tftp", tags: ["tftp"] }
    - { role: "chrony", tags: ["chrony"] }
