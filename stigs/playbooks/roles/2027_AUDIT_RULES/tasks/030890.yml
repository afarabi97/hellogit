# DISA-STIG-RHEL-07-030890
- name: "Search {{ audit_directory }} for other DAC audit rules"
  find:
    paths: "{{ audit_directory }}"
    recurse: false
    contains: -F key=delete$
    patterns: '*.rules'
  register: find_renameat

- name: "If existing DAC ruleset not found, use {{ audit_directory }}/delete.rules as the recipient for the rule"
  set_fact:
    all_files:
    - "{{ audit_directory }}/delete.rules"
  when:
  - find_renameat.matched is defined and find_renameat.matched == 0

- name: Use matched file as the recipient for the rule
  set_fact:
    all_files:
    - '{{ find_renameat.files | map(attribute=''path'') | list | first }}'
  when:
  - find_renameat.matched is defined and find_renameat.matched > 0

- name: "Inserts/replaces the renameat rule in {{ audit_directory }} on (x86)"
  lineinfile:
    path: '{{ all_files[0] }}'
    line: -a always,exit -F arch=b32 -S renameat -F auid>=1000 -F auid!=unset -F key=delete
    create: true

- name: "Inserts/replaces the renameat rule in {{ audit_directory }} (x86_64)"
  lineinfile:
    path: '{{ all_files[0] }}'
    line: -a always,exit -F arch=b64 -S renameat -F auid>=1000 -F auid!=unset -F key=delete
    create: true
