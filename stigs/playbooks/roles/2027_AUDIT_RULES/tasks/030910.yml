# DISA-STIG-RHEL-07-030910
- name: "Search {{ audit_directory }} for other DAC audit rules"
  find:
    paths: "{{ audit_directory }}"
    recurse: false
    contains: -F key=delete$
    patterns: '*.rules'
  register: find_unlink

- name: "If existing DAC ruleset not found, use {{ audit_directory }}/delete.rules as the recipient for the rule"
  set_fact:
    all_files:
    - "{{ audit_directory }}/delete.rules"
  when:
  - find_unlink.matched is defined and find_unlink.matched == 0

- name: Use matched file as the recipient for the rule
  set_fact:
    all_files:
    - '{{ find_unlink.files | map(attribute=''path'') | list | first }}'
  when:
  - find_unlink.matched is defined and find_unlink.matched > 0

- name: "Inserts/replaces the unlink rule in {{ audit_directory }} (x86)"
  lineinfile:
    path: '{{ all_files[0] }}'
    line: -a always,exit -F arch=b32 -S unlink -F auid>=1000 -F auid!=unset -F key=delete
    create: true

- name: "Inserts/replaces the unlink rule in {{ audit_directory }} (x86_64)"
  lineinfile:
    path: '{{ all_files[0] }}'
    line: -a always,exit -F arch=b64 -S unlink -F auid>=1000 -F auid!=unset -F key=delete
    create: true
