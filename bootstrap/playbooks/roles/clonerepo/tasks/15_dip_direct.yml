---

- name: "Download direct download files"
  get_url:
    url: "{{ item.url }}/{{ item.name }}"
    dest: "{{ item.dest }}"
    mode: "{{ item.mode }}"
  with_items:
    - "{{ direct_download_package_names }}"

