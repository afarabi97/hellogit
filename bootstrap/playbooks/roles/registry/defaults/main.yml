%YAML 1.1
---

#### Registry configuration variables ####
# All Registry configuration variables for Registry install itself.

firewall_port: "5000"

registry_config: "/etc/docker-distribution/registry/config.yml"
