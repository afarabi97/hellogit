{
    "insecure-registries" : ["https://{{ server_ip }}:{{ firewall_port }}","{{ server_ip }}:{{ firewall_port }}"],
    "registry-mirrors": ["https://{{ server_ip }}:{{ firewall_port }}","http://{{ server_ip }}:{{ firewall_port }}"]
}
