---

- name: Setup up project defined aliases
  lineinfile:
    path: "/root/.bashrc"
    regexp: '^source .*aliases$'
    line: 'source /opt/tfplenum/aliases'

- name: write out bootstrap vars
  template:
    src: templates/bootstrap_vars.yml
    dest: "{{ BOOTSTRAP_VARS }}"
