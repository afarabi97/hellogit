---
- name: Add tfplenum shibd se exception
  copy:
    src: "shibd.pp"
    dest: "/tmp/"

- name: Execute se shibd/httpd module
  command: semodule -i /tmp/shibd.pp
  register: result
  changed_when: result.rc == 0

- name: Remove shibd.pp
  file:
    path: /tmp/shibd.pp
    state: absent
