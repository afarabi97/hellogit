---

#  ceph requires rpm python-urllib3 which conflicts with pip version
- name: remove urllib3
  pip:
    name: urllib3
    state: absent

- name: Install ceph-deploy
  yum:
    name: "ceph-deploy"
    state: installed
    allow_downgrade: yes
  when: inventory_hostname in groups['master-server']

...
