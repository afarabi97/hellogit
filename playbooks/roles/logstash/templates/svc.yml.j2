apiVersion: v1
kind: Service
metadata:
  name: logstash
  labels:
    component: logstash
spec:
  clusterIP: None
  selector:
    component: logstash
  ports:
  - port: {{ logstash_port }}
    protocol: TCP
  type: LoadBalancer
