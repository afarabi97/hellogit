#######################################################
###################  Kubernetes Repo ###############
#######################################################
---

#- name: "Setup Kubernetes Online Repo"
#  yum_repository:
#    baseurl: "{{ kube_online.baseurl }}"
#    description: "{{ kube_online.description }}"
#    gpgcheck: "{{ kube_online.gpgcheck }}"
#    gpgkey: "{{ kube_online.gpgkey }}"
#    name: "{{ kube_online.name }}"
#    enabled: true
#  when: not offline or (offline is defined and offline == false)

- debug:
    msg: "{{ kube_offline.baseurl }}"

- name: "Setup Kubernetes Offline Repo"
  yum_repository:
    baseurl: "{{ kube_offline.baseurl }}"
    description: "{{ kube_offline.description }}"
    gpgcheck: "{{ kube_offline.gpgcheck }}"
    gpgkey: "{{kube_offline.gpgkey }}"
    name: "{{ kube_offline.name }}"
    enabled: true
    file: "offline"
#  when: offline is defined and offline

...
