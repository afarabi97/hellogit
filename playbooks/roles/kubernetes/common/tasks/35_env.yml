---
- name: 'Configure Kubelet'
  lineinfile:
    path: /etc/sysconfig/kubelet
    regexp: "^KUBELET_EXTRA_ARGS="
    line: "KUBELET_EXTRA_ARGS=--system-reserved cpu=1,memory=800Mi --kube-reserved cpu=200m,memory=200Mi"
  notify: restart kubelet
