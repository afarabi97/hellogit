######################################################
################# Main Bro ###########################
######################################################

- import_tasks: 05_scripts.yml
  when: inventory_hostname in groups['bro']
  tags:
    - bro-scale

- import_tasks: 10_install.yml
  when: inventory_hostname in groups['master-server']