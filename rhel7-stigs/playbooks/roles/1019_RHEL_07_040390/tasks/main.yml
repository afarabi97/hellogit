- name: "RHEL-07-040390"
  lineinfile:
      dest: /etc/ssh/sshd_config
      regexp: "(?i)^#?Protocol"
      line: Protocol 2
      validate: /usr/sbin/sshd -t -f %s
  notify: restart sshd
  tags:
      - RHEL-07-040390
      - ssh
