- name: RHEL-07-040170"
  lineinfile:
      dest: /etc/ssh/sshd_config
      regexp: ^#?Banner
      line: Banner /etc/issue.net
      validate: /usr/sbin/sshd -tf %s
  notify: restart sshd
  tags:
      - RHEL-07-040170
      - ssh
      - dod_logon_banner
