- name: >
    "RHEL-07-010050"
  template:
    src: ./templates/logon-banner.j2
    dest: "{{ item }}"
    owner: root
    group: root
    mode: 0644
  with_items:
    - /etc/issue.in
    - /etc/issue.net
    - /etc/issue
  tags:
    - RHEL-07-010050
    - ssh
    - dod_logon_banner

