Clear Staging Environment:
  stage: clean-staging-env
  tags:
    - tfplenum-buildv2
  script: |
    echo "IP of runner is $(hostname -I | cut -d " " -f1)"
    echo "Current working directory is $(pwd)"
    echo "Clear Staging Environment: $DRIVE_STAGING_FOLDER/"
    rm -fr "$DRIVE_STAGING_FOLDER/"
    mkdir -p "$DRIVE_STAGING_FOLDER/"
  retry: 2
  only:
    variables:
      - $PIPELINE == "export-docs"
      - $PIPELINE == "export-all"
      - $PIPELINE == "minor-release"

Clear GIP Staging Environment:
  stage: clean-staging-env
  tags:
    - tfplenum-buildv2
  script: |
    echo "IP of runner is $(hostname -I | cut -d " " -f1)"
    echo "Current working directory is $(pwd)"
    echo "Clear Staging Environment"
    rm -fr "$DRIVE_GIP_STAGING_FOLDER/"
    mkdir -p "$DRIVE_GIP_STAGING_FOLDER/"
  retry: 2
  only:
    variables:
      - $PIPELINE == "export-gip"

Hash Master GIP Folder:
  stage: hashfiles
  tags:
    - tfplenum-buildv2
  script: |
    python3 testing/pipeline/pipeline.py run-export create-master-drive-hashes \
    --create-drive-type "GIP" \
    --drive-creation-version "$TFPLENUM_EXPORT_VERSION" \
    --username "$MASTER_DRIVE_CREATION_USERNAME" \
    --password "$MASTER_DRIVE_CREATION_PASSWORD" \
    --ipaddress "$MASTER_DRIVE_CREATION_IPADDRESS"
  only:
    variables:
      - $PIPELINE == "export-gip"
      - $PIPELINE == "drive-only" && $CREATE_DRIVE_TYPE == "GIP" && $EXPORT_PERFORM_CHECKSUM == "yes"

Hash Master CPT Folder:
  stage: hashfiles
  tags:
    - tfplenum-buildv2
  script: |
    python3 testing/pipeline/pipeline.py run-export create-master-drive-hashes \
    --create-drive-type "CPT" \
    --drive-creation-version "$TFPLENUM_EXPORT_VERSION" \
    --username "$MASTER_DRIVE_CREATION_USERNAME" \
    --password "$MASTER_DRIVE_CREATION_PASSWORD" \
    --ipaddress "$MASTER_DRIVE_CREATION_IPADDRESS"
  only:
    variables:
      - $PIPELINE == "export-all"
      - $PIPELINE == "minor-release"
      - $PIPELINE == "drive-only" && $CREATE_DRIVE_TYPE == "Mixed" && $EXPORT_PERFORM_CHECKSUM == "yes"
      - $PIPELINE == "drive-only" && $CREATE_DRIVE_TYPE == "CPT" && $EXPORT_PERFORM_CHECKSUM == "yes"
      - $PIPELINE == "export-controller-only"

Hash Master MDT Folder:
  stage: hashfiles
  tags:
    - tfplenum-buildv2
  script: |
    python3 testing/pipeline/pipeline.py run-export create-master-drive-hashes \
    --create-drive-type "MDT" \
    --drive-creation-version "$TFPLENUM_EXPORT_VERSION" \
    --username "$MASTER_DRIVE_CREATION_USERNAME" \
    --password "$MASTER_DRIVE_CREATION_PASSWORD" \
    --ipaddress "$MASTER_DRIVE_CREATION_IPADDRESS"
  only:
    variables:
      - $PIPELINE == "export-all"
      - $PIPELINE == "minor-release"
      - $PIPELINE == "drive-only" && $CREATE_DRIVE_TYPE == "Mixed" && $EXPORT_PERFORM_CHECKSUM == "yes"
      - $PIPELINE == "drive-only" && $CREATE_DRIVE_TYPE == "MDT" && $EXPORT_PERFORM_CHECKSUM == "yes"
      - $PIPELINE == "export-controller-only"

Create Drives:
  stage: publish
  tags:
    - tfplenum-buildv2
  script: |
    python3 testing/pipeline/pipeline.py run-export create-drives \
    --create-drive-type "$CREATE_DRIVE_TYPE" \
    --username "$MASTER_DRIVE_CREATION_USERNAME" \
    --password "$MASTER_DRIVE_CREATION_PASSWORD" \
    --ipaddress "$MASTER_DRIVE_CREATION_IPADDRESS" \
    --drive-creation-version "$TFPLENUM_EXPORT_VERSION" \
    --burn-multiboot $CREATE_MULTIBOOT
  only:
    variables:
      - $PIPELINE == "export-all"
      - $PIPELINE == "minor-release"
      - $PIPELINE == "drive-only"
      - $PIPELINE == "export-controller-only"
      - $PIPELINE == "export-gip"

Check Drives:
  stage: checkhash
  tags:
    - tfplenum-buildv2
  script: |
    python3 testing/pipeline/pipeline.py run-export check-master-drive-hashes \
    --create-drive-type "$CREATE_DRIVE_TYPE" \
    --drive-creation-version "$TFPLENUM_EXPORT_VERSION" \
    --username "$MASTER_DRIVE_CREATION_USERNAME" \
    --password "$MASTER_DRIVE_CREATION_PASSWORD" \
    --ipaddress "$MASTER_DRIVE_CREATION_IPADDRESS" \
    --burn-multiboot $CREATE_MULTIBOOT
  only:
    variables:
      - $PIPELINE == "export-all"
      - $PIPELINE == "minor-release"
      - $PIPELINE == "drive-only"
      - $PIPELINE == "export-controller-only"
      - $PIPELINE == "export-gip"
