Clear Staging Environment:
  stage: clean-staging-env
  tags:
    - tfplenum-buildv2
  script: |
    echo "IP of runner is $(hostname -I | cut -d " " -f1)"
    echo "Current working directory is $(pwd)"
    echo "Clear Staging Environment: $DRIVE_STAGING_FOLDER/"
    rm -fr "$DRIVE_STAGING_FOLDER/"
    mkdir -p "$DRIVE_STAGING_FOLDER/"
  retry: 2
  only:
    variables:
      - $PIPELINE == "export-powershell-modules"
      - $PIPELINE == "export-docs"
      - $PIPELINE == "export-all"
      - $PIPELINE == "minor-release"

Clear GIP Staging Environment:
  stage: clean-staging-env
  tags:
    - tfplenum-buildv2
  script: |
    echo "IP of runner is $(hostname -I | cut -d " " -f1)"
    echo "Current working directory is $(pwd)"
    echo "Clear Staging Environment"
    rm -fr "$DRIVE_GIP_STAGING_FOLDER/"
    mkdir -p "$DRIVE_GIP_STAGING_FOLDER/"
  retry: 2
  only:
    variables:
      - $RUN_CREATE_MASTER_DRIVE == "yes" && $PIPELINE == "export-gip"

Hash Master GIP Drive:
  stage: hashfiles
  tags:
    - tfplenum-buildv2
  script: |
    python3 testing/pipeline/pipeline.py run-export create-master-drive-hashes \
    --drive-type "GIP" \
    --drive-creation-version "v$TFPLENUM_EXPORT_VERSION" \
    --username "$MASTER_DRIVE_CREATION_USERNAME" \
    --password "$MASTER_DRIVE_CREATION_PASSWORD" \
    --ipaddress "$MASTER_DRIVE_CREATION_IPADDRESS"
  only:
    variables:
      - $RUN_CREATE_MASTER_DRIVE == "yes" && $PIPELINE == "export-gip"

Hash Check Master GIP Drive:
  stage: checkhash
  tags:
    - tfplenum-buildv2
  script: |
    python3 testing/pipeline/pipeline.py run-export check-master-drive-hashes \
    --drive-type "GIP" \
    --drive-creation-version "v$TFPLENUM_EXPORT_VERSION" \
    --username "$MASTER_DRIVE_CREATION_USERNAME" \
    --password "$MASTER_DRIVE_CREATION_PASSWORD" \
    --ipaddress "$MASTER_DRIVE_CREATION_IPADDRESS"
  only:
    variables:
      - $RUN_CREATE_MASTER_DRIVE == "yes" && $PIPELINE == "export-gip"

Hash Master CPT Drive:
  stage: hashfiles
  tags:
    - tfplenum-buildv2
  script: |
    python3 testing/pipeline/pipeline.py run-export create-master-drive-hashes \
    --drive-type "CPT" \
    --drive-creation-version "v$TFPLENUM_EXPORT_VERSION" \
    --username "$MASTER_DRIVE_CREATION_USERNAME" \
    --password "$MASTER_DRIVE_CREATION_PASSWORD" \
    --ipaddress "$MASTER_DRIVE_CREATION_IPADDRESS"
  only:
    variables:
      - $PIPELINE == "export-all" && $RUN_CREATE_MASTER_DRIVE == "yes"
      - $PIPELINE == "minor-release"
      - $RUN_CREATE_MASTER_DRIVE == "yes" && $PIPELINE == "drive-only"

Hash Master MDT Drive:
  stage: hashfiles
  tags:
    - tfplenum-buildv2
  script: |
    python3 testing/pipeline/pipeline.py run-export create-master-drive-hashes \
    --drive-type "MDT" \
    --drive-creation-version "v$TFPLENUM_EXPORT_VERSION" \
    --username "$MASTER_DRIVE_CREATION_USERNAME" \
    --password "$MASTER_DRIVE_CREATION_PASSWORD" \
    --ipaddress "$MASTER_DRIVE_CREATION_IPADDRESS"
  only:
    variables:
      - $PIPELINE == "export-all" && $RUN_CREATE_MASTER_DRIVE == "yes"
      - $MDT == "yes" && $PIPELINE == "minor-release"
      - $RUN_CREATE_MASTER_DRIVE == "yes" && $PIPELINE == "drive-only"

Hash Check Master CPT Drive:
  stage: checkhash
  tags:
    - tfplenum-buildv2
  script: |
    python3 testing/pipeline/pipeline.py run-export check-master-drive-hashes \
    --drive-type "CPT" \
    --drive-creation-version "v$TFPLENUM_EXPORT_VERSION" \
    --username "$MASTER_DRIVE_CREATION_USERNAME" \
    --password "$MASTER_DRIVE_CREATION_PASSWORD" \
    --ipaddress "$MASTER_DRIVE_CREATION_IPADDRESS"
  only:
    variables:
      - $PIPELINE == "export-all" && $RUN_CREATE_MASTER_DRIVE == "yes"
      - $PIPELINE == "minor-release"
      - $RUN_CREATE_MASTER_DRIVE == "yes" && $PIPELINE == "drive-only"

Hash Check Master MDT Drive:
  stage: checkhash
  tags:
    - tfplenum-buildv2
  script: |
    python3 testing/pipeline/pipeline.py run-export check-master-drive-hashes \
    --drive-type "MDT" \
    --drive-creation-version "v$TFPLENUM_EXPORT_VERSION" \
    --username "$MASTER_DRIVE_CREATION_USERNAME" \
    --password "$MASTER_DRIVE_CREATION_PASSWORD" \
    --ipaddress "$MASTER_DRIVE_CREATION_IPADDRESS"
  only:
    variables:
      - $PIPELINE == "export-all" && $RUN_CREATE_MASTER_DRIVE == "yes"
      - $MDT == "yes" && $PIPELINE == "minor-release"
      - $RUN_CREATE_MASTER_DRIVE == "yes" && $PIPELINE == "drive-only"

Create Master CPT Drive:
  stage: publish
  tags:
    - tfplenum-buildv2
  script: |
    python3 testing/pipeline/pipeline.py run-export create-master-drive \
    --external-drive "$MASTER_DRIVE_CREATION_EXTERNAL_CPT_DRIVE" \
    --drive-type "CPT" \
    --username "$MASTER_DRIVE_CREATION_USERNAME" \
    --password "$MASTER_DRIVE_CREATION_PASSWORD" \
    --ipaddress "$MASTER_DRIVE_CREATION_IPADDRESS" \
    --drive-creation-version "v$TFPLENUM_EXPORT_VERSION" \
    --burn-multiboot $CREATE_MULTIBOOT
  only:
    variables:
      - $RUN_CREATE_MASTER_DRIVE == "yes" && $PIPELINE == "export-all"
      - $RUN_CREATE_MASTER_DRIVE == "yes" && $PIPELINE == "minor-release"
      - $RUN_CREATE_MASTER_DRIVE == "yes" && $PIPELINE == "drive-only"

Create Master MDT Drive:
  stage: publish
  tags:
    - tfplenum-buildv2
  script: |
    python3 testing/pipeline/pipeline.py run-export create-master-drive \
    --external-drive "$MASTER_DRIVE_CREATION_EXTERNAL_MDT_DRIVE" \
    --drive-type "MDT" \
    --username "$MASTER_DRIVE_CREATION_USERNAME" \
    --password "$MASTER_DRIVE_CREATION_PASSWORD" \
    --ipaddress "$MASTER_DRIVE_CREATION_IPADDRESS" \
    --drive-creation-version "v$TFPLENUM_EXPORT_VERSION" \
    --burn-multiboot $CREATE_MULTIBOOT
  only:
    variables:
      - $RUN_CREATE_MASTER_DRIVE == "yes" && $PIPELINE == "export-all"
      - $RUN_CREATE_MASTER_DRIVE == "yes" && $MDT == "yes" && $PIPELINE == "minor-release"
      - $RUN_CREATE_MASTER_DRIVE == "yes" && $PIPELINE == "drive-only"

Create Master GIP Drive:
  stage: publish
  tags:
    - tfplenum-buildv2
  script: |
    python3 testing/pipeline/pipeline.py run-export create-master-drive \
    --external-drive "$MASTER_DRIVE_CREATION_EXTERNAL_GIP_DRIVE" \
    --drive-type "GIP" \
    --username "$MASTER_DRIVE_CREATION_USERNAME" \
    --password "$MASTER_DRIVE_CREATION_PASSWORD" \
    --ipaddress "$MASTER_DRIVE_CREATION_IPADDRESS" \
    --drive-creation-version "v$TFPLENUM_EXPORT_VERSION" \
    --burn-multiboot "no"
  only:
    variables:
      - $RUN_CREATE_MASTER_DRIVE == "yes" && $PIPELINE == "export-gip"
