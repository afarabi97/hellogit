Clear Staging Environment:
  stage: clean-staging-env
  tags:
    - tfplenum-buildv2
  script: |
    echo "IP of runner is $(hostname -I | cut -d " " -f1)"
    echo "Current working directory is $(pwd)"
    echo "Clear Staging Environment"
    rm -fr "$DRIVE_STAGING_FOLDER/"
    mkdir -p "$DRIVE_STAGING_FOLDER/"
  retry: 2
  only:
    variables:
      - $PIPELINE == "export-docs"
      - $PIPELINE == "export-all"

Clear GIP Staging Environment:
  stage: clean-staging-env
  tags:
    - tfplenum-buildv2
  script: |
    echo "IP of runner is $(hostname -I | cut -d " " -f1)"
    echo "Current working directory is $(pwd)"
    echo "Clear Staging Environment"
    rm -fr "$DRIVE_GIP_STAGING_FOLDER/"
    mkdir -p "$DRIVE_GIP_STAGING_FOLDER/"
  retry: 2
  only:
    variables:
      - $PIPELINE == "export-gip"


Hash Master GIP Drive:
  stage: hashfiles
  tags:
    - tfplenum-buildv2
  script: |
    python3 testing/pipeline/pipeline.py run-export create-master-drive-hashes \
    --rsync-source "$MASTER_DRIVE_CREATION_GIP_RSYNC_SOURCE"
  only:
    variables:
      - $PIPELINE == "export-gip"


Hash Check Master GIP Drive:
  stage: checkhash
  tags:
    - tfplenum-buildv2
  script: |
    python3 testing/pipeline/pipeline.py run-export check-master-drive-hashes \
    --rsync-source "$MASTER_DRIVE_CREATION_GIP_RSYNC_SOURCE"
  only:
    variables:
      - $PIPELINE == "export-gip"


Hash Master CPT Drive:
  stage: hashfiles
  tags:
    - tfplenum-buildv2
  script: |
    python3 testing/pipeline/pipeline.py run-export create-master-drive-hashes \
    --rsync-source "$MASTER_DRIVE_CREATION_CPT_RSYNC_SOURCE"
  only:
    variables:
      - $PIPELINE == "export-all"


Hash Master MDT Drive:
  stage: hashfiles
  tags:
    - tfplenum-buildv2
  script: |
    python3 testing/pipeline/pipeline.py run-export create-master-drive-hashes \
    --rsync-source "$MASTER_DRIVE_CREATION_MDT_RSYNC_SOURCE"
  only:
    variables:
      - $PIPELINE == "export-all"


Hash Check Master CPT Drive:
  stage: checkhash
  tags:
    - tfplenum-buildv2
  script: |
    python3 testing/pipeline/pipeline.py run-export check-master-drive-hashes \
    --rsync-source "$MASTER_DRIVE_CREATION_CPT_RSYNC_SOURCE"
  only:
    variables:
      - $PIPELINE == "export-all"


Hash Check Master MDT Drive:
  stage: checkhash
  tags:
    - tfplenum-buildv2
  script: |
    python3 testing/pipeline/pipeline.py run-export check-master-drive-hashes \
    --rsync-source "$MASTER_DRIVE_CREATION_MDT_RSYNC_SOURCE"
  only:
    variables:
      - $PIPELINE == "export-all"


Create Master CPT Drive:
  stage: publish
  tags:
    - tfplenum-buildv2
  script: |
    python3 testing/pipeline/pipeline.py run-export create-master-drive \
    --external-drive "$MASTER_DRIVE_CREATION_EXTERNAL_CPT_DRIVE" \
    --mount-point "$MASTER_DRIVE_CREATION_CPT_MOUNT_POINT" \
    --rsync-source "$MASTER_DRIVE_CREATION_CPT_RSYNC_SOURCE" \
    --username "$MASTER_DRIVE_CREATION_USERNAME" \
    --password "$MASTER_DRIVE_CREATION_PASSWORD" \
    --ipaddress "$MASTER_DRIVE_CREATION_IPADDRESS" \
    --multiboot-path "$MASTER_DRIVE_CPT_MULTIBOOT_PATH" \
    --drive-creation-path "$MASTER_DRIVE_CREATION_PATH" \
    --drive-creation-version "v$TFPLENUM_EXPORT_VERSION" \
    --burn-multiboot $CREATE_MULTIBOOT
  only:
    variables:
      - $RUN_CREATE_MASTER_DRIVE == "yes"


Create Master MDT Drive:
  stage: publish
  tags:
    - tfplenum-buildv2
  script: |
    python3 testing/pipeline/pipeline.py run-export create-master-drive \
    --external-drive "$MASTER_DRIVE_CREATION_EXTERNAL_MDT_DRIVE" \
    --mount-point "$MASTER_DRIVE_CREATION_MDT_MOUNT_POINT" \
    --rsync-source "$MASTER_DRIVE_CREATION_MDT_RSYNC_SOURCE" \
    --username "$MASTER_DRIVE_CREATION_USERNAME" \
    --password "$MASTER_DRIVE_CREATION_PASSWORD" \
    --ipaddress "$MASTER_DRIVE_CREATION_IPADDRESS" \
    --drive-creation-path "$MASTER_DRIVE_CREATION_PATH" \
    --multiboot-path "$MASTER_DRIVE_MDT_MULTIBOOT_PATH" \
    --drive-creation-version "v$TFPLENUM_EXPORT_VERSION" \
    --burn-multiboot $CREATE_MULTIBOOT
  only:
    variables:
      - $RUN_CREATE_MASTER_DRIVE == "yes"


Create Master GIP Drive:
  stage: publish
  tags:
    - tfplenum-buildv2
  script: |
    python3 testing/pipeline/pipeline.py run-export create-master-drive \
    --external-drive "$MASTER_DRIVE_CREATION_EXTERNAL_GIP_DRIVE" \
    --mount-point "$MASTER_DRIVE_CREATION_GIP_MOUNT_POINT" \
    --rsync-source "$MASTER_DRIVE_CREATION_GIP_RSYNC_SOURCE" \
    --username "$MASTER_DRIVE_CREATION_USERNAME" \
    --password "$MASTER_DRIVE_CREATION_PASSWORD" \
    --ipaddress "$MASTER_DRIVE_CREATION_IPADDRESS" \
    --drive-creation-path "$MASTER_DRIVE_CREATION_PATH" \
    --drive-creation-version "v$TFPLENUM_EXPORT_VERSION" \
    --burn-multiboot no
  only:
    variables:
      - $RUN_CREATE_GIP_DRIVE == "yes"
