{
    "name": "Winlogbeat",
    "form": [
        {
            "name": "winlog_beat_dest_ip",
            "label": "Traffic Destination",
            "description": "If you have a DIP setup with a PF Sense firewall, please enter the external IP Address of that here. If not, please provide the Kuberenetes service IP Address. When entering the PF sense firewall, keep in mind that targets from outside of the DIP must go through this firewall and the administrator of that firewall will need to enable port forwarding to the appropriate kubectl services.",
            "type": "textinput",
            "required": true,
            "regexp": "^((2[0-2][0-3])|(1\\d\\d)|([1-9]?\\d))(\\.((25[0-5])|(2[0-4]\\d)|(1\\d\\d)|([1-9]?\\d))){2}\\.((25[0-4])|(2[0-4]\\d)|(1\\d\\d)|([1-9]?\\d))$",
            "default_value": ""
        },
        {
            "name": "winlog_beat_dest_port",
            "label": "Traffic Destination Port",
            "type": "textinput",
            "required": true,
            "default_value": "5045"
        }
    ]
}
