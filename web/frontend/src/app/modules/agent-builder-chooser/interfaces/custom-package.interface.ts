import { PackageInterface } from './package.interface';

/**
 * Interface defines the Custom Package
 *
 * @export
 * @interface CustomPackageInterface
 */
export interface CustomPackageInterface {
  [ name: string ]: PackageInterface;
}
