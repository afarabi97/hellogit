/**
 * Interface defines system version
 *
 * @export
 * @interface SystemVersionInterface
 */
export interface SystemVersionInterface {
  version: string;
  commit_hash: string;
  build_date: string;
}
