/**
 * Interface defines the Hive Form
 *
 * @export
 * @interface HiveFormInterface
 */
export interface HiveFormInterface {
  event_title: string;
  event_tags: string;
  event_severity: string;
  event_description: string;
}
