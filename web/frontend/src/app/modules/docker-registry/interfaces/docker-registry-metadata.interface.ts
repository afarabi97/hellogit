/**
 * Interface defines the docker registry metadata
 *
 * @export
 * @interface DockerRegistryMetadataInterface
 */
export interface DockerRegistryMetadataInterface {
  tag: string;
  image_id: string;
  image_size: number;
}
