/**
 * Interface defines the Colors Dict
 *
 * @export
 * @interface ColorsDictInterface
 */
export interface ColorsDictInterface {
  deployed: string;
  'pending install': string;
  uninstalling: string;
  uninstalled: string;
  failed: string;
}
