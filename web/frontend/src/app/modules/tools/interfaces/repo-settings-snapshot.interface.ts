/**
 * Interface defines the Repo Settings Snapshot
 *
 * @export
 * @interface RepoSettingsSnapshotInterface
 */
export interface RepoSettingsSnapshotInterface {
  endpoint: string;
  protocol: string;
  bucket: string;
  access_key: string;
  secret_key: string;
}
