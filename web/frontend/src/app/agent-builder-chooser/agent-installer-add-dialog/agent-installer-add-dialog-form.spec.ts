import { AgentInstallerAddDialogForm } from './agent-installer-add-dialog-form';

describe('AgentInstallerAddDialogForm', () => {
  it('should create an instance', () => {
    expect(new AgentInstallerAddDialogForm()).toBeTruthy();
  });
});
