/**
 * Enum defines notification status
 *
 * @export
 * @enum {number}
 */
// eslint-disable-next-line no-shadow
export enum NotificationStatusEnum {
  STARTED = 'STARTED',
  COMPLETED = 'COMPLETED',
  IN_PROGRESS = 'IN_PROGRESS',
}
