/**
 * Interface defines the default ipv4 settings
 *
 * @export
 * @interface DefaultIPV4SettingsInterface
 */
export interface DefaultIPV4SettingsInterface {
  address: string;
  alias: string;
  broadcast: string;
  gateway: string;
  interface: string;
  macaddress: string;
  mtu: number;
  netmask: string;
  network: string;
  type: string;
}
