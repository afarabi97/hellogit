import { ProcessFromFormGroupInterface } from './process-from-form-group.interface';

/**
 * Interface defines the Role Process
 *
 * @export
 * @interface RoleProcessInterface
 */
export interface RoleProcessInterface {
  role: string;
  process: ProcessFromFormGroupInterface;
}
